name: mobile
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.5.0
  record: ^6.1.2
  record_windows: ^1.0.7
  path_provider: ^2.1.5
  flutter_tts: ^3.8.0
  shared_preferences: ^2.3.2   # Demo/Premium & sayaçlar için
  in_app_purchase: ^3.1.8      # Store ödemeleri için eklendi

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/pexels_images/
